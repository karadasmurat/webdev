<!DOCTYPE html>
<html lang="en">

<head>
    <!-- To reduce duplication, we will leverage EJS partials. -->
    <%- include('../partials/head.ejs') %>
    <title>Create Campground</title>
</head>

<body>
    <div class="row">
        <div class="col-6 offset-3">
            <h1>Create Campground</h1>
            <form action="/campgrounds" method="POST">

                <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" name="title" id="title" value="<%=campground.title%>"
                        placeholder="Campground Title" class="form-control">
                </div>
                <div class="mb-3">
                    <label for="location" class="form-label">Location</label>
                    <input type="text" name="location" id="location" value="<%=campground.location%>"
                        placeholder="Location" class="form-control">
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Price</label>
                    <div class="input-group mb-3">
                        <span class="input-group-text">$</span>
                        <input type="number" name="price" id="price" value="<%=campground.price%>" placeholder="0.00"
                            class="form-control" aria-label="Amount (to the nearest dollar)">
                        <!-- <span class="input-group-text">.00</span> -->
                    </div>
                </div>
                <div class="mb-3">
                    <label for="image" class="form-label">Image URL</label>
                    <input type="text" name="image" id="image" value="<%=campground.image%>" placeholder="Image URL"
                        class="form-control">
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea type="text" name="description" id="description" placeholder="Description"
                        class="form-control"><%=campground.description%></textarea>
                </div>

                <div class="d-flex justify-content-end">
                    <a href="/campgrounds" class="btn btn-outline-secondary mx-2">Cancel</a>
                    <button type="submit" class="btn btn-success">Save</button>
                </div>

            </form>
            <%- include('../partials/footer.ejs') %>
        </div>

    </div>

</body>

</html>